{% extends "base.twig" %}

{% block content %}
    {{ include('components/headerDeco.twig') }}
    <div class="c-container">
		<div class="c-container__inner l-gap">
			<div>
				<h3 class="text-h3 text-pri">{{ post.title }}</h3>
			</div>
			<div class="mt-20 editorPanel">
				{{ post.content }}
			</div>
		</div>
	</div>
{% endblock %}