{% extends "base.twig" %}

{% block content %}
	{{ include('components/headerDeco.twig') }}
	<div class="c-container">
		<div class="c-container__inner l-gap">
			<div>
                <h3 class="text-h3">{{ post.title }}</h3>
            </div>
			{{ include('components/navibar.twig') }}
			<div class="l-gap-editor editorPanel">
				{{ post.content }}
			</div>
		</div>
	</div>
{% endblock %}
